Write a function that displays at the left margin of the screen a solid square of asterisks whose side is specified in integerparameter side. 


#include<iostream>
using namespace std;

void square(int);
int main()
{
    int side;
    cout<<" Enter side : ";
    cin>>side;
    square(side);
    return 0;
}
void square(int side)
{
    for (int i=0;i<side;i++)
    {
        for(int j=0;j<side;j++)
        {
            cout<<"*";
        }
        cout<<"\n";
    }
}
